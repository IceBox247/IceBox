// Function entry point used when Vercel's Root Directory is the repo root.
// The implementation is shared with `server/api/index.ts` — see handler.ts.
export { default } from '../server/src/handler';
